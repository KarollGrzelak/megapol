services:
  - type: web
    name: megapol-server
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npx tsx server/index.ts
    envVars:
      - key: PORT
        value: 10000
